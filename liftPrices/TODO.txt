1- CELINE
	x Création éléments graphiques (coeur pour les vies ,numéro d'étage ,ascenseur plus mur, intérieur d'ascenseur, porte, bouton plus et moins).

2- VALENTIN
lift.js :
	x Mise en place des éléments (porte devant le produit, texte etc).
	x Creation des evenements
	x function createLift() : crée l'ascenseur
	x function doorLift(Boolean) : vraix ouvre les portes, faux ferme les portes
	x function upFloor() : change d'étage
	x function up() : Bouton plus de l'ascenseur
	x function down() : Bouton moins de l'ascenseur

3- CELINE
product.js :
	x function getRandProduct() : stock un produit aléatoire
	x function checkPrice(String) : param "+" ou "-", compare le prix réel au faux prix au param "+" ou "-"
	x function displayProduct(Boolean) : affiche ou pas le produit selectionné

	x creer des exemples de produits dans le data.js.
	x faire les fonctions dans product.js ( placement de l'objet dans le jeu, et affichage du prix)
	x tester en utilisant le fichier game.js avec le createGame.

	x retailler les photos
	x placer image dans acsenseur
	x nom produit au dessus de la photo
	x photo
	x prix estimé:2€
	x ce produit vaut il plus ou moins?
	x checkprice fonctionnel
	

4- VALENTIN
game.js :
	x Creation de la mécanique de jeu et évènement
	- function winFloor() : quand on gagne sur l'étage
	- function looseFloor()	: quand on perd
	- function gameWin() : gagne la partie et indique a idol qu'on a gagné un prix
	- function gameLoose() : perd la partie

5 - CELINE
status.js :
	- function displayWinGame(Boolean) Boolean true = affiche, false = desactive (utilise methode destroy pour cela, voir le lift.js)
	- function displayWin(Boolean)
	- function displayLoose(Boolean)
	- function displayLooseGame(Boolean);	